{
  "name": "careercoded-journal-management-system-v2",
  "version": "1.0.0",
  "private": true,
  "description": "Alternative monolithic CareerCoded journal management system with a distinct editorial UI.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "dev": "npm run build && npm run dev --prefix server",
    "start": "npm run build && npm run start --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
